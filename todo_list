1、Done （!高）完善增量训练功能
2、Done（低）完善可观测性追踪工程化配置
3、Done（!高）增加元数据同步功能，通过数据库访问每日同步历史元数据
4、（低）增加数据血缘同步功能
5、（低）扩展知识库功能，支持关联数据血缘的任务或者工作流
6、Done（!高）完善主题域功能，选择主题域后限制向量检索的范围
7、Done（!高）项目打包，迁移到内网
8、Done（中）规范化sql生成提示词，将提示词公共内容提取出来维护，支持多层级提示词
9、Done（中）SQL后校验，不允许有insert，update，delete，drop，create等关键词
10、Done（低）人在回路，当不清晰的时候允许人来了补充内容
11、（低）高复杂回路，将输入拆为多个子任务，然后再并发生成，最终组装成完整sql
12、Done（低）chromadb切换到qdrant


改进点：
2.待定：使用图数据库，其中关联关系就是字段间的边，血缘关系就是表之间的边
3.向量检索已有的问答对，来找到以前的相似sql（并发）
4.在生成sql节点提供工具：例如样例数据查询工具，关联关系查询工具，字典码查询工具等
5.提示词修改：返回的字段名都as成中文，尽量使用with，必须使用etl date
7.？？生成多个sql，选择最好的一个


1、对接DS可以生成工作流，可以补数 Done
2、对接SPARK SQL，可以预执行
3、动态生成宽表，每次指标任务上下线则同步宽表数据
4、模型允许配置一个帐号规则和一个客户规则 Done(不需要)
5、模型配置允许导入导出 Doing
6、实时指标计算时候需要支持使用离线指标的数据
7、模型命中数据后落表，按配置发送消息或者管控
8、生成duckdb的查询条件
9、对接kafka

1、录入模型的时候不用写id
2、需要处理同步任务，不能因为同步任务报错导致主任务失败
3、每次上线版本号需要增加
4、任务上线后可以修改SQL
5、同步目标表后需要修改历史宽表状态
6、模型回测需要展示回测结果，跳过了也要展示
7、模型回测sql需要cast


为什么每次都是超时了，而不是正常结束