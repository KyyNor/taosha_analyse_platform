# tests/backend/

> 本目录存放 **淘沙分析平台** 后端所有未推送提交内容对应的 **单元测试**（.py）。
> 覆盖范围：纯 Python 逻辑、Schema/Pydantic 校验、布尔条件真值表。不包含端到端测试。

---

## 📁 目录结构

```
tests/backend/
├── __init__.py                ← 统一 sys.path（确保 backend/* 可 import）
├── conftest.py                ← 全局 fixtures（Mocks、factories）
├── pytest.ini                 ← pytest 配置（标记、自定义路径）
├── README.md                  ← 本文件
└── modules/                   ← 按功能模块组织的测试
    ├── wide_table_sync/           ← WT 系列（宽表增量同步）
    │   └── test_wide_table_incremental.py
    ├── realtime_indicator/        ← RT 系列（实时指标半小时间隔）
    │   └── test_compute_half_hour_slot.py
    ├── alert_notification/        ← ALERT 系列 + MODEL 系列
    │   ├── test_notification_merge.py     ← 通知目标合并去重（ALERT-01~10, R01）
    │   └── test_risk_model_switch.py       ← 理财经理开关真假值表（MODEL-01~06）
    ├── province_cardbin/          ← PCB 系列（省市卡BIN维表）
    │   └── test_province_cardbin_schema.py ← Schema 校验 + 业务逻辑分支
    ├── history_trend/              ← HIST 系列（历史模型趋势分析）
    │   └── test_model_history_trend.py     ← TrendRequest 校验 + 聚合逻辑 Mock
    ├── alert_control_record_optimization/  ← ACR 系列（页面优化）
    │   └── test_alert_control_record_page.py
    └── indicator_management/      ← IND 系列（指标任务联动）
        └── test_indicator_task_linkage.py
```

> **注意**：QUAL 系列（代码质量专项）和非功能性测试暂未纳入，原因是它们依赖运行时环境（Prometheus/Grafana、日志文件），建议纳入 CI/CD 的 Health Check 阶段。

---

## 🎯 测试策略总纲

| 分类 | 覆盖内容 | 测试文件 | 占比 |
|------|---------|---------|------|
| ✅ 直接可测 | Pydantic Schema、纯函数、真值表、参数化边界 | 多数 `test_*.py` | ~50% |
| ⚠️ 需一次小改造 | 嵌入式业务逻辑（通过抽取纯函数可立即可测） | `test_wide_table_incremental.py` | ~25% |
| ⛔ 客观 UT 不适合 | 真实 DB 双写、真实 HTTP 外调、E2E 页面行为 | 各 SKIP 占位 | ~25% |

---

## 🚦 SKIP 占位约定

凡有 `@pytest.mark.skip` 的测试，其 `reason=` 都严格说明：
1. **跳过原因**（具体指出为何不适合 UT）
2. **建议的替代测试手段**（Integration / E2E / Manual Smoke）
3. **测试责任人**（通常是 QA 团队的集成测试计划）

---

## ⚙️ 改造前置清单（需开发负责人审批后实施）

以下是本次**最重要的结构性改动建议**，列于此处供开发负责人审阅。

### 改造 1 — 宽表增量同步：`WideTableComparator` 抽取
- **文件**：`backend/services/fraudhunter/wide_table_service/sync_service.py`
- **目标**：将 `_diff_indicators()` 抽取为独立领域类 `WideTableComparator.diff()`
- **收益**：WT-02/03/04 系列（当前 @skip）瞬间可测（~10 秒执行完 20+ 用例）
- **影响范围**：仅删除原函数体内的 ~30 行，迁移到新建文件，零破坏
- **状态**：改造清单已在本 `modules/wide_table_sync/` 中的 `REFACTORING_PLAN` 常量中声明

### 改造 2 — 通知合并：`merge_notification_targets()` 抽取
- **文件**：`backend/services/fraudhunter/model_service/model_hit_alert_manager.py`
- **目标**：将 `hit_record_processor()` 第 262-270 行的去重逻辑暴露为 `@staticmethod`
- **收益**：ALERT-01~05 全系列无 Mock 可测（纯算术，~5ms 执行完）
- **影响范围**：在同类内新增一个 public static method，约 15 行
- **状态**：改造清单已在本 `modules/alert_notification/` 中的 `TEST_REFACTORING_HINT` 常量中声明

### 改造 3（可选）— QueryExecutor 接口 + Mock 实现
- **背景**：Spark 执行路径难以在 CI 环境 Mock，长期解耦可考虑 Strategy 模式
- **投入产出比**：改造投入较大（~200行），适用于多版本共存的中长期工程
- **优先级**：低（短期先用集成测试覆盖，长期有需要再做）

---

## 🧪 运行指引

```bash
# 根目录运行所有后端UT
cd /home/data_center/whwangzeqi/taosha_analyse_platform
pytest tests/backend/ -v

# 仅运行某模块（例如 WT 场景）
pytest tests/backend/modules/wide_table_sync/ -v -m wt

# 仅运行即时可测用例（跳过所有 SKIP 和改造前置）
pytest tests/backend/modules/realtime_indicator/ -v
pytest tests/backend/modules/alert_notification/test_risk_model_switch.py -v

# 仅运行 Schema 层测试（最快，~50个用例秒级完成）
pytest tests/backend/modules/ -v -k "Schema or Switch or TruthTable or ACard or History"

# 列出所有测试用例及状态
pytest tests/backend/ --collect-only -q
```

---

## 📋 与测试用例文档的对应关系

| 文档章节 | 模块 | 对应测试文件 | 当前状态 |
|---------|------|-------------|---------|
| 一、宽表增量同步 | WT | `wide_table_sync/test_wide_table_incremental.py` | 需改造后激活 |
| 二、实时指标半小时间隔 | RT | `realtime_indicator/test_compute_half_hour_slot.py` | ✅ 可直接运行 |
| 三、省市卡BIN | PCB | `province_cardbin/test_province_cardbin_schema.py` | ✅ 部分可测，CRUD 需 E2E |
| 四、多通知目标合并 | ALERT | `alert_notification/test_notification_merge.py` | ✅ 部分可测，核心需改造 |
| 四（补充）理财经理开关 | MODEL | `alert_notification/test_risk_model_switch.py` | ✅ Schema 可测 |
| 五、告警管控记录页面优化 | ACR | `alert_control_record_optimization/test_alert_control_record_page.py` | ✅ Schema 可测，权限 E2E |
| 六、历史模型分析 | HIST | `history_trend/test_model_history_trend.py` | ✅ 校验可测，聚合需 Mock |
| 七、指标管理与指标任务联动 | IND | `indicator_management/test_indicator_task_linkage.py` | ✅ 列表 API 可测 |
| 八、风控模型配置增强 | MODEL | （同上 MODEL 系列） | ✅ 已在上述覆盖 |
| 九、代码质量专项 | QUAL | （无 UT，建议 CI Health Check） | ⊘ SKIP |
| 十、非功能性测试 | — | （集成/压测/E2E 范畴） | ⊘ SKIP |

---

*Generated by Claude Code from test documents dated 2026/04/22.
 All test stubs marked as SKIP contain rationale and suggested alternatives.*