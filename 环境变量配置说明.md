# 环境变量配置说明

## 快速开始

1. **复制模板文件**
   ```bash
   cp .env.example .env
   ```

2. **编辑配置文件**
   ```bash
   # Windows
   notepad .env
   
   # Linux/Mac
   nano .env
   ```

3. **配置 OpenAI API**
   - 在 `.env` 文件中填入你的 API 密钥
   - 如果使用代理或自定义服务，修改 `OPENAI_BASE_URL`

## 支持的AI服务

### 1. OpenAI GPT 系列
```env
OPENAI_API_KEY=sk-your_openai_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
```

### 2. 阿里云通义千问
```env
OPENAI_API_KEY=sk-your_dashscope_key_here
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
```

### 3. 其他兼容 OpenAI 格式的服务
```env
OPENAI_API_KEY=your_api_key
OPENAI_BASE_URL=https://your-service.com/v1
```

## 配置验证

启动应用后，检查日志中是否有 API 配置相关的错误信息。

## 安全注意事项

- ⚠️ **永远不要将 `.env` 文件提交到 git**
- 🔒 **API 密钥是敏感信息，请妥善保管**
- 📝 **只提交 `.env.example` 模板文件**